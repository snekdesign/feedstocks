--- a/Cargo.toml
+++ b/Cargo.toml
@@ -9,10 +9,10 @@
 codegen-units = 1
 lto = true
 strip = true
-opt-level = "z"
+opt-level = 3

 [features]
-default = ["native-tls"]
+default = ["rustls-tls"]
 native-tls = [
   "reqwest/native-tls",
   "reqwest/native-tls-alpn",
@@ -56,6 +56,9 @@
 ] }
 url = "2.5.8"

+[build-dependencies]
+thunk-rs = { version = "0.3.5", features = ["win7"], default-features = false }
+
 [dev-dependencies]
 rstest = "0.26.1"
 assert_cmd = "2.2.2"
