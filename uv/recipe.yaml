context:
  version: 0.12.1
  sha256: 4e87a6c0e5feddb55e080ba2e8b7ecc1379361471d724071b0c0e0ba5df72981

package:
  name: uv
  version: ${{ version }}

source:
  url: https://codeload.github.com/astral-sh/uv/tar.gz/${{ version }}
  sha256: ${{ sha256 }}
  patches:
  - changes.patch

build:
  number: 0
  script:
    content:
    - if: win
      then:
      - set AWS_LC_SYS_PREBUILT_NASM=0
      - cargo install --features windows-gui-bin --locked --no-track --path crates\uv --root "%LIBRARY_PREFIX%"
      else:
      - cargo install --locked --no-track --path crates/uv --root "$PREFIX"
    env:
      CARGO_HOME: ${{ env.get('CARGO_HOME', default='') }}
  skip:
  - build_platform != target_platform
  - win and not (compiler('c') is startingwith('vs'))

requirements:
  build:
  - ${{ compiler('c') }}
  - rust >=1.94.0
  - if: linux
    then: diffutils
  - if: win
    then: nasm
  - if: microarch_level|int > 0
    then: x86_64-microarch-level ==${{ microarch_level }}

tests:
- script:
  - uv -V

about:
  license: MIT
  license_file: LICENSE-MIT
  summary: An extremely fast Python package installer and resolver, written in Rust.
