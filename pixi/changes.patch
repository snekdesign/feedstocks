--- a/.cargo/config.toml
+++ b/.cargo/config.toml
@@ -4,10 +4,6 @@
 [target.x86_64-pc-windows-msvc]
 linker = "rust-lld"

-# Statically link the C runtime so the executable does not depend on the MSVC runtime DLLs.
-[target.'cfg(all(target_env = "msvc", target_os = "windows"))']
-rustflags = ["-C", "target-feature=+crt-static"]
-
 [target.'cfg(all(windows, debug_assertions))']
 rustflags = [
   # increase the stack size to prevent overflowing the stack in debug
--- a/Cargo.toml
+++ b/Cargo.toml
@@ -232,6 +232,7 @@
 rattler_conda_types = { version = "0.46", default-features = false, features = [
   "rayon",
 ] }
+rattler_config = { version = "0.4.1", default-features = false }
 rattler_digest = { version = "1.3", default-features = false }
 rattler_index = { version = "0.30", default-features = false, features = [
   "s3",
@@ -255,6 +256,9 @@
 simple_spawn_blocking = { version = "1.1.0", default-features = false }

 # Rattler build crates
+rattler-build = { git = "https://github.com/prefix-dev/rattler-build", rev = "59ae2f4cc1962fa2a3f1d5e5976b5da78e9d0085", default-features = false, features = [
+  "s3",
+] }
 rattler_build_core = { version = "0.2.4", default-features = false, features = [
   "s3",
 ] }
@@ -264,6 +268,32 @@
 rattler_build_variant_config = { version = "0.1.6" }

 [patch.crates-io]
+coalesced_map = { path = "../rattler/crates/coalesced_map" }
+file_url = { path = "../rattler/crates/file_url" }
+path_resolver = { path = "../rattler/crates/path_resolver" }
+rattler = { path = "../rattler/crates/rattler" }
+rattler_cache = { path = "../rattler/crates/rattler_cache" }
+rattler_conda_types = { path = "../rattler/crates/rattler_conda_types" }
+rattler_config = { path = "../rattler/crates/rattler_config" }
+rattler_digest = { path = "../rattler/crates/rattler_digest" }
+rattler_git = { path = "../rattler/crates/rattler_git" }
+rattler_index = { path = "../rattler/crates/rattler_index" }
+rattler_lock = { path = "../rattler/crates/rattler_lock" }
+rattler_macros = { path = "../rattler/crates/rattler_macros" }
+rattler_menuinst = { path = "../rattler/crates/rattler_menuinst" }
+rattler_networking = { path = "../rattler/crates/rattler_networking" }
+rattler_package_streaming = { path = "../rattler/crates/rattler_package_streaming" }
+rattler_prefix_guard = { path = "../rattler/crates/rattler_prefix_guard" }
+rattler_pty = { path = "../rattler/crates/rattler_pty" }
+rattler_redaction = { path = "../rattler/crates/rattler_redaction" }
+rattler_repodata_gateway = { path = "../rattler/crates/rattler_repodata_gateway" }
+rattler_s3 = { path = "../rattler/crates/rattler_s3" }
+rattler_shell = { path = "../rattler/crates/rattler_shell" }
+rattler_solve = { path = "../rattler/crates/rattler_solve" }
+rattler_upload = { path = "../rattler/crates/rattler_upload" }
+rattler_virtual_packages = { path = "../rattler/crates/rattler_virtual_packages" }
+simple_spawn_blocking = { path = "../rattler/crates/simple_spawn_blocking" }
+
 version-ranges = { git = "https://github.com/astral-sh/pubgrub", rev = "d8efd77673c9a90792da9da31b6c0da7ea8a324b" }

 # Redirect crates.io's `reqwest-middleware` to a local shim that re-exports
@@ -276,15 +306,15 @@
 # Temporary patch: rattler-build main has been updated to use the new rattler
 # versions but no release has been published yet. Remove once rattler-build
 # cuts a new release.
-rattler_build_core = { git = "https://github.com/prefix-dev/rattler-build", branch = "main" }
-rattler_build_jinja = { git = "https://github.com/prefix-dev/rattler-build", branch = "main" }
-rattler_build_networking = { git = "https://github.com/prefix-dev/rattler-build", branch = "main" }
-rattler_build_recipe = { git = "https://github.com/prefix-dev/rattler-build", branch = "main" }
-rattler_build_script = { git = "https://github.com/prefix-dev/rattler-build", branch = "main" }
-rattler_build_source_cache = { git = "https://github.com/prefix-dev/rattler-build", branch = "main" }
-rattler_build_types = { git = "https://github.com/prefix-dev/rattler-build", branch = "main" }
-rattler_build_variant_config = { git = "https://github.com/prefix-dev/rattler-build", branch = "main" }
-rattler_build_yaml_parser = { git = "https://github.com/prefix-dev/rattler-build", branch = "main" }
+rattler_build_core = { git = "https://github.com/prefix-dev/rattler-build", rev = "59ae2f4cc1962fa2a3f1d5e5976b5da78e9d0085" }
+rattler_build_jinja = { git = "https://github.com/prefix-dev/rattler-build", rev = "59ae2f4cc1962fa2a3f1d5e5976b5da78e9d0085" }
+rattler_build_networking = { git = "https://github.com/prefix-dev/rattler-build", rev = "59ae2f4cc1962fa2a3f1d5e5976b5da78e9d0085" }
+rattler_build_recipe = { git = "https://github.com/prefix-dev/rattler-build", rev = "59ae2f4cc1962fa2a3f1d5e5976b5da78e9d0085" }
+rattler_build_script = { git = "https://github.com/prefix-dev/rattler-build", rev = "59ae2f4cc1962fa2a3f1d5e5976b5da78e9d0085" }
+rattler_build_source_cache = { git = "https://github.com/prefix-dev/rattler-build", rev = "59ae2f4cc1962fa2a3f1d5e5976b5da78e9d0085" }
+rattler_build_types = { git = "https://github.com/prefix-dev/rattler-build", rev = "59ae2f4cc1962fa2a3f1d5e5976b5da78e9d0085" }
+rattler_build_variant_config = { git = "https://github.com/prefix-dev/rattler-build", rev = "59ae2f4cc1962fa2a3f1d5e5976b5da78e9d0085" }
+rattler_build_yaml_parser = { git = "https://github.com/prefix-dev/rattler-build", rev = "59ae2f4cc1962fa2a3f1d5e5976b5da78e9d0085" }

 [patch."https://github.com/prefix-dev/rattler-build"]
 #rattler-build = { path = "/var/home/tobias/src/rattler-build" }
--- a/crates/pixi_api/src/context.rs
+++ b/crates/pixi_api/src/context.rs
@@ -34,7 +34,7 @@
     /// Search for packages matching a [`MatchSpec`]
     pub async fn search(
         &self,
-        matchspec: MatchSpec,
+        matchspec: Vec<MatchSpec>,
         channels: IndexSet<Channel>,
         platforms: Vec<Platform>,
     ) -> miette::Result<Vec<RepoDataRecord>> {
@@ -420,7 +420,7 @@
     /// Search for packages matching a [`MatchSpec`]
     pub async fn search(
         &self,
-        matchspec: MatchSpec,
+        matchspec: Vec<MatchSpec>,
         channels: IndexSet<Channel>,
         platforms: Vec<Platform>,
     ) -> miette::Result<Vec<RepoDataRecord>> {
--- a/crates/pixi_api/src/workspace/search/mod.rs
+++ b/crates/pixi_api/src/workspace/search/mod.rs
@@ -7,7 +7,7 @@

 pub async fn search(
     workspace: Option<&Workspace>,
-    matchspec: MatchSpec,
+    matchspec: Vec<MatchSpec>,
     channels: IndexSet<Channel>,
     platforms: Vec<Platform>,
 ) -> miette::Result<Vec<RepoDataRecord>> {
@@ -21,7 +21,7 @@
     let gateway = config.gateway().with_client(client).finish();

     let repo_data = gateway
-        .query(channels.clone(), platforms, vec![matchspec.clone()])
+        .query(channels.clone(), platforms, matchspec.clone())
         .recursive(false)
         .await
         .into_diagnostic()?;
@@ -33,11 +33,18 @@
     }

     if packages.is_empty() {
-        return Err(miette::miette!(
-            help = "Try glob patterns like 'python*' or '*numpy*'",
-            "No packages found matching '{}'",
-            matchspec
-        ));
+        if matchspec.len() == 1 {
+            return Err(miette::miette!(
+                help = "Try glob patterns like 'python*' or '*numpy*'",
+                "No packages found matching '{}'",
+                matchspec[0]
+            ));
+        } else {
+            return Err(miette::miette!(
+                help = "Try glob patterns like 'python*' or '*numpy*'",
+                "No packages found",
+            ));
+        }
     }

     packages.sort();
--- a/crates/pixi_build_backend/Cargo.toml
+++ b/crates/pixi_build_backend/Cargo.toml
@@ -23,6 +23,7 @@
 minijinja = { workspace = true }
 ordermap = { workspace = true }
 pathdiff = { workspace = true }
+rattler-build = { workspace = true }
 rattler_build_core = { workspace = true }
 rattler_build_jinja = { workspace = true }
 rattler_build_recipe = { workspace = true }
@@ -29,6 +30,7 @@
 rattler_build_types = { workspace = true }
 rattler_build_variant_config = { workspace = true }
 rattler_conda_types = { workspace = true }
+rattler_config = { workspace = true }
 rattler_digest = { workspace = true }
 rattler_virtual_packages = { workspace = true }
 serde = { workspace = true, features = ["derive"] }
@@ -42,6 +44,7 @@
 url = { workspace = true }

 pixi_build_types = { workspace = true }
+pixi_config = { workspace = true }

 jsonrpc-core = { workspace = true }
 jsonrpc-http-server = { workspace = true }
--- a/crates/pixi_build_backend/src/intermediate_backend.rs
+++ b/crates/pixi_build_backend/src/intermediate_backend.rs
@@ -26,7 +26,7 @@
     console_utils::LoggingOutputHandler,
     metadata::{BuildConfiguration, Output, PlatformWithVirtualPackages},
     system_tools::SystemTools,
-    tool_configuration::Configuration,
+    tool_configuration::{Configuration, reqwest_client_from_auth_storage},
     types::{Directories, PackageIdentifier, PackagingSettings},
 };
 use rattler_build_recipe::variant_render::RenderConfig;
@@ -824,10 +824,36 @@
             .await
             .into_diagnostic()?;

+        let base_client = {
+            let config = pixi_config::Config::load_global();
+            let mut config_base = rattler_config::config::ConfigBase::default();
+            config_base.mirrors.extend(config.mirrors.into_iter());
+            config_base.s3_options.0.extend(
+                config.s3_options.into_iter().map(move |(k, v)| {
+                    (k, rattler_config::config::s3::S3Options {
+                        endpoint_url: v.endpoint_url,
+                        region: v.region,
+                        force_path_style: v.force_path_style,
+                    })
+                })
+            );
+            let common = rattler_build::opt::CommonData::from_opts_and_config(
+                Default::default(),
+                config_base,
+            );
+            reqwest_client_from_auth_storage(
+                None,
+                common.s3_config,
+                common.mirror_config,
+                None,
+            ).into_diagnostic()?
+        };
+
         let tool_config = Configuration::builder()
             .with_opt_cache_dir(self.cache_dir.clone())
             .with_logging_output_handler(self.logging_output_handler.clone())
             .with_testing(false)
+            .with_reqwest_client(base_client)
             // Pixi is incremental so keep the build
             .with_keep_build(true)
             // This indicates that the environments are externally managed, e.g. they are already
--- a/crates/pixi_build_rattler_build/Cargo.toml
+++ b/crates/pixi_build_rattler_build/Cargo.toml
@@ -18,10 +18,12 @@
 indexmap = { workspace = true }
 miette = { workspace = true }
 pathdiff = { workspace = true }
+rattler-build = { workspace = true }
 rattler_build_core = { workspace = true }
 rattler_build_recipe = { workspace = true }
 rattler_build_variant_config = { workspace = true }
 rattler_conda_types = { workspace = true }
+rattler_config = { workspace = true }
 serde = { workspace = true, features = ["derive"] }
 serde_json = { workspace = true }
 tempfile = { workspace = true }
@@ -31,6 +33,7 @@

 chrono = "0.4.41"
 pixi_build_types = { workspace = true }
+pixi_config = { workspace = true }
 tracing.workspace = true

 [dev-dependencies]
--- a/crates/pixi_build_rattler_build/src/protocol.rs
+++ b/crates/pixi_build_rattler_build/src/protocol.rs
@@ -34,7 +34,7 @@
     build::{WorkingDirectoryBehavior, run_build},
     console_utils::LoggingOutputHandler,
     metadata::{BuildConfiguration, Output, PlatformWithVirtualPackages},
-    tool_configuration::Configuration,
+    tool_configuration::{Configuration, reqwest_client_from_auth_storage},
     types::{PackageIdentifier, PackagingSettings},
 };
 use rattler_build_recipe::{stage1::Source as RecipeSource, variant_render::RenderConfig};
@@ -447,10 +447,36 @@
             self.recipe_source.path.clone(),
         );

+        let base_client = {
+            let config = pixi_config::Config::load_global();
+            let mut config_base = rattler_config::config::ConfigBase::default();
+            config_base.mirrors.extend(config.mirrors.into_iter());
+            config_base.s3_options.0.extend(
+                config.s3_options.into_iter().map(move |(k, v)| {
+                    (k, rattler_config::config::s3::S3Options {
+                        endpoint_url: v.endpoint_url,
+                        region: v.region,
+                        force_path_style: v.force_path_style,
+                    })
+                })
+            );
+            let common = rattler_build::opt::CommonData::from_opts_and_config(
+                Default::default(),
+                config_base,
+            );
+            reqwest_client_from_auth_storage(
+                None,
+                common.s3_config,
+                common.mirror_config,
+                None,
+            ).into_diagnostic()?
+        };
+
         let tool_config = Configuration::builder()
             .with_opt_cache_dir(self.cache_dir.clone())
             .with_logging_output_handler(self.logging_output_handler.clone())
             .with_testing(false)
+            .with_reqwest_client(base_client)
             // Pixi is incremental so keep the build
             .with_keep_build(true)
             // This indicates that the environments are externally managed, e.g. they are already
--- a/crates/pixi_cli/src/search.rs
+++ b/crates/pixi_cli/src/search.rs
@@ -12,7 +12,7 @@
 use pixi_manifest::FeaturesExt;
 use pixi_progress::await_in_progress;
 use rattler_conda_types::{
-    MatchSpec, PackageName, ParseStrictness, ParseStrictnessWithNameMatcher, Platform,
+    MatchSpec, PackageName, PackageNameMatcher, ParseStrictness, ParseStrictnessWithNameMatcher, Platform,
     RepoDataRecord,
 };
 use tracing::{debug, error};
@@ -31,7 +31,7 @@
 pub struct Args {
     /// MatchSpec of a package to search
     #[arg(required = true)]
-    pub package: String,
+    pub package: Vec<String>,

     #[clap(flatten)]
     pub channels: ChannelsConfig,
@@ -49,7 +49,7 @@
     pub platform: Option<Platform>,

     /// Limit the number of versions shown per package, -1 for no limit
-    #[clap(short, long, default_value = "5", allow_hyphen_values = true)]
+    #[clap(short, long, default_value = "-1", allow_hyphen_values = true)]
     pub limit: i64,

     /// Limit the number of packages shown, -1 for no limit
@@ -56,7 +56,7 @@
     #[clap(
         short = 'n',
         long = "limit-packages",
-        default_value = "5",
+        default_value = "-1",
         allow_hyphen_values = true
     )]
     pub limit_packages: i64,
@@ -126,22 +126,31 @@
         }
         platforms
     } else {
-        Platform::all().collect()
+        vec![
+            Platform::Linux64,
+            Platform::LinuxAarch64,
+            Platform::NoArch,
+            Platform::Osx64,
+            Platform::OsxArm64,
+            Platform::Win64,
+        ]
     };

-    let matchspec = MatchSpec::from_str(
-        &args.package,
-        ParseStrictnessWithNameMatcher {
-            parse_strictness: ParseStrictness::Lenient,
-            exact_names_only: false,
-        },
-    )
-    .into_diagnostic()?;
+    let matchspec = args.package.into_iter().map(
+            |p| MatchSpec::from_str(
+                &p,
+                ParseStrictnessWithNameMatcher {
+                    parse_strictness: ParseStrictness::Lenient,
+                    exact_names_only: false,
+                },
+            )
+        ).collect::<Result<Vec<_>, _>>()
+        .into_diagnostic()?;

     let packages = if let Some(workspace) = workspace {
         await_in_progress("searching packages...", |_| async {
             WorkspaceContext::new(CliInterface {}, workspace)
-                .search(matchspec, channels, platforms)
+                .search(matchspec.clone(), channels, platforms)
                 .await
         })
         .await?
@@ -148,7 +157,7 @@
     } else {
         await_in_progress("searching packages...", |_| async {
             DefaultContext::new(CliInterface {})
-                .search(matchspec, channels, platforms)
+                .search(matchspec.clone(), channels, platforms)
                 .await
         })
         .await?
@@ -164,7 +173,16 @@
         }
     } else {
         let limit_versions = if args.limit < 0 {
-            None
+            if matchspec.into_iter().all(
+                |p| match p.name {
+                    PackageNameMatcher::Exact(_) => true,
+                    _ => false,
+                }
+            ) {
+                None
+            } else {
+                Some(1)
+            }
         } else {
             Some(args.limit as usize)
         };
--- a/crates/pixi_global/src/install.rs
+++ b/crates/pixi_global/src/install.rs
@@ -55,7 +55,7 @@
             } else {
                 false
             }
-        })
+        }).or_else(|| matching_executables.first())
     } else {
         matching_executables.first()
     };
--- a/crates/pixi_global/src/project/parsed_manifest.rs
+++ b/crates/pixi_global/src/project/parsed_manifest.rs
@@ -391,11 +391,7 @@
     type Err = ExposedNameError;

     fn from_str(value: &str) -> Result<Self, Self::Err> {
-        if value == pixi_utils::executable_name() {
-            Err(ExposedNameError::PixiBinParseError)
-        } else {
-            Ok(ExposedName(value.to_string()))
-        }
+        Ok(ExposedName(value.to_string()))
     }
 }

--- a/crates/pixi_uv_conversions/Cargo.toml
+++ b/crates/pixi_uv_conversions/Cargo.toml
@@ -13,6 +13,7 @@
 [dependencies]
 chrono = { workspace = true }
 dunce = { workspace = true }
+http = { workspace = true }
 jiff = { workspace = true }
 miette = { workspace = true }
 pep440_rs = { workspace = true }
--- a/crates/pixi_uv_conversions/src/conversions.rs
+++ b/crates/pixi_uv_conversions/src/conversions.rs
@@ -5,6 +5,7 @@

 use crate::GitUrlWithPrefix;
 use crate::git_url::decode_windows_drive_letter;
+use http::HeaderValue;
 use miette::IntoDiagnostic;
 use pep440_rs::VersionSpecifiers;
 use pixi_git::{git::GitReference as PixiGitReference, sha::GitSha as PixiGitSha};
@@ -23,7 +24,8 @@
 use uv_configuration::BuildOptions;
 use uv_configuration::TrustedHost;
 use uv_distribution_types::{
-    ConfigSettingEntry, ConfigSettings, GitSourceDist, Index, IndexLocations, IndexUrl,
+    ConfigSettingEntry, ConfigSettings, GitSourceDist, Index, IndexCacheControl, IndexLocations,
+    IndexUrl,
 };
 use uv_normalize::{InvalidNameError, PackageName};
 use uv_pep508::{VerbatimUrl, VerbatimUrlError};
@@ -98,6 +100,17 @@
         ));
     }

+    let cache_control = Some(
+        IndexCacheControl {
+            api: Some(
+                HeaderValue::from_static("max-age=600"),
+            ),
+            files: Some(
+                HeaderValue::from_static("max-age=365000000, immutable, public"),
+            ),
+        },
+    );
+
     // Convert the index to a `IndexUrl`
     let index = options
         .index_url
@@ -105,7 +118,11 @@
         .map(DisplaySafeUrl::from_url)
         .map(VerbatimUrl::from_url)
         .map(IndexUrl::from)
-        .map(Index::from_index_url)
+        .map(|url| {
+            let mut index = Index::from_index_url(url);
+            index.cache_control = cache_control.clone();
+            index
+        })
         .into_iter();

     // Convert to list of extra indexes
@@ -118,7 +135,11 @@
                 .map(DisplaySafeUrl::from_url)
                 .map(VerbatimUrl::from_url)
                 .map(IndexUrl::from)
-                .map(Index::from_extra_index_url)
+                .map(|url| {
+                    let mut index = Index::from_extra_index_url(url);
+                    index.cache_control = cache_control.clone();
+                    index
+                })
         });

     let flat_indexes = if let Some(flat_indexes) = options.find_links.clone() {
