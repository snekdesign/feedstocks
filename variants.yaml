zip_keys:
- [c_compiler, c_compiler_version]
- [cxx_compiler, cxx_compiler_version]

c_compiler:
- gcc
- if: win
  then: vs

c_compiler_version:
- '14.2'
- if: win
  then: '2022.12'

cxx_compiler:
- gxx
- if: win
  then: vs

cxx_compiler_version:
- '14.2'
- if: win
  then: '2022.12'

cdt_name:
- cos7
